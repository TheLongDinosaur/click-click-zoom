import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Click Click Zoom</h1>
        <p>Click on a car, but don't click the same car twice!</p>
    </div>
);

export default Title;